# Root .env — used by docker-compose.prod.yml for MySQL initialization
# Copy to .env and fill in real values before running docker compose
#
# cp .env.example .env

DB_PASSWORD=your_mysql_root_password
DB_NAME=l9trends
